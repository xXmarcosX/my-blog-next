export type PostModel = {
  id: string,
  slug: string,
  title: string,
  author: string,
  excerpt: string,
  content: string,
  coverImageUrl: string,
  published: boolean,
  createdAt: string,
  updatedAt: string,
}